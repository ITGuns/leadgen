# BLOCKERS.md

Real-mode items parked until a human provides them. Nothing here blocks development — everything runs in `MOCK_MODE=1`.

| # | item | needed for | who/where (see procurement guide §) | state |
|---|---|---|---|---|
| B1 | ~~`PAGESPEED_API_KEY`~~ | real website-quality scoring | — | ✅ DONE 2026-09-13: key validated live (Lighthouse run), installed as Vercel env for prod+preview; PageSpeed stage active on new runs, backfill via campaign Retry |
| B2 | Google OAuth client (`GOOGLE_OAUTH_CLIENT_ID/SECRET`) + `GOOGLE_DRIVE_FOLDER_ID` + one-time consent in Settings | Drive exports + backup push | same project (§4); **never a bare service account** | ⏸ waiting |
| B3 | `CF_TUNNEL_TOKEN`, `CF_ACCESS_TEAM_DOMAIN`, `CF_ACCESS_AUD` | production access/auth — **Docker/tunnel path only**; the chosen Vercel path uses B13–B15 + `AUTH_TRUST_PLATFORM` (D20) instead | Cloudflare Zero Trust (§5) | ⏸ optional (alternative path) |
| B4 | Host (≥4 GB RAM, ≥40 GB disk, Docker) | **Docker path only** (superseded by Vercel+Supabase, D19); still needed as *any workstation* for the monthly DuckDB extract | Hetzner/DO or office box (§6) | ⏸ optional (alternative path) — for scale reference, the REAL pipeline is proven locally: DE = 50,425 businesses in ~30s total (extract 18s + conflate 12s), gate passed, FSQ skipped gracefully without a token |
| B5 | ~~Overture release + taxonomy file~~ | real ingest + category catalog | — | ✅ DONE 2026-09-07: release pinned (2026-08-19.0); the canonical taxonomy CSV vanished from Overture's schema repo, so the catalog (1,792 codes) is now **derived from the release data itself** (`scripts/derive-taxonomy.ts`, D18) and niche mappings/fixtures/goldens are reconciled to the real vocabulary. Proven: real DE ingest gates at 0.00% unresolved. On release bump: re-run the derive script |
| B6 | FSQ access — probed live 2026-09-06: the guide's "anonymous" S3 bucket now serves only LICENSE/NOTICE; distribution is the **gated HF dataset** `foursquare/fsq-os-places` (gated=auto: create a free HF account, accept terms, generate a token). Latest release `dt=2026-08-11` (monthly cadence); canonical path `hf://datasets/foursquare/fsq-os-places/release/dt=<REL>/places/parquet/*.parquet`; column mapping verified against Foursquare's schema docs. **Remaining human step:** HF account → `HF_TOKEN` in `.env` or Settings | real gap-fill ingest | huggingface.co → foursquare/fsq-os-places | ✅ DONE 2026-09-13: operator account created, gated terms accepted, fine-grained token validated (file-level 206 read), installed as Vercel env; first real DE gap-fill executed (61,157 FSQ rows staged, matcher run) |
| B7 | *(optional)* `ANTHROPIC_API_KEY` + console spend limit (~$20) | AI owner extraction + taxonomy assist | console.anthropic.com (§7) | ⏸ optional |
| B8 | *(optional)* `OUTSCRAPER_API_KEY` + prepaid credits | paid coverage top-up | outscraper.com (§8) | ⏸ optional |
| B9 | *(optional)* full SimpleMaps US Cities basic CSV over `data/us_cities.csv` | top-up city fan-out beyond the bundled starter | simplemaps.com/data/us-cities — **site bot-blocks scripted fetches (403), so this is a 30-second browser download by a human**; drop the unzipped `uscities.csv` over `data/us_cities.csv` (loader reads by header) | ⏸ optional — starter subset bundled |
| B10 | `APP_SECRET` (any long random string) in prod `.env` | encrypting stored OAuth token / keys at rest | generated by operator | ⏸ waiting |
| B11 | ~~Live-bucket verification of the real Overture extract SQL~~ | de-risk D9 | `npx tsx scripts/probe-overture.ts` | ✅ DONE 2026-09-03: anonymous access works; `DESCRIBE` of release 2026-08-19.0 matches the coded mapping (names.primary, taxonomy.primary/alternates — *alternates* plural was the one drift, fixed; addresses[].region arrives as `TX`; operating_status nullable; phones unnormalized — all handled). FSQ probe still pending B6 |
| B12 | Gemfield client roster import (domains/phones) | do-not-prospect suppression seeded | ops → Suppressions → Import (kind=client) | ⏸ waiting |
| B13 | **Supabase project** → `DATABASE_URL` (use the **transaction pooler** string, `…pooler.supabase.com:6543`) + `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` | production database + export artifact storage (D19) | supabase.com → New project; Settings → Database → Connection string → *Transaction pooler*; Settings → API for the URL + service-role key. **SIZING (learned live 2026-09-09):** the free tier's 500 MB holds roughly ONE small state (DE ≈ 100 MB); TX alone stages 1.2 GB and TX+FL+GA lands 3–5 GB → needs **Pro ($25/mo, 8 GB)**. Overflow puts the project into READ-ONLY enforcement (the app now survives that and serves reads; cleanup = `BEGIN; SET TRANSACTION READ WRITE; TRUNCATE places_overture; COMMIT`) | ✅ project live (free tier, operator chose small-state scope for now) |
| B14 | Supabase Storage bucket **`exports`** (private) | durable export downloads on Vercel (D19) | Supabase dashboard → Storage → New bucket, name `exports`, public OFF | ⏸ waiting |
| B15 | **Vercel project** linked to this repo + env vars (`MOCK_MODE`, `DATABASE_URL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET` = any long random string, `APP_SECRET`, `AUTH_TRUST_PLATFORM=1` + `OPERATOR_EMAIL` behind Deployment Protection — D20, plus the data/API keys from B1–B8 as they arrive) | hosting + minute cron for jobs (D19) | vercel.com → Add New Project → import repo (`vercel.json` already declares the cron; cron needs the app deployed to prod) | ⏸ waiting — **deploy only on the operator's explicit go** |
