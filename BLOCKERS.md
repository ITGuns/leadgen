# BLOCKERS.md

Real-mode items parked until a human provides them. Nothing here blocks development — everything runs in `MOCK_MODE=1`.

| # | item | needed for | who/where (see procurement guide §) | state |
|---|---|---|---|---|
| B1 | `PAGESPEED_API_KEY` | real website-quality scoring | Google Cloud console, PageSpeed API (§3) | ⏸ waiting |
| B2 | Google OAuth client (`GOOGLE_OAUTH_CLIENT_ID/SECRET`) + `GOOGLE_DRIVE_FOLDER_ID` + one-time consent in Settings | Drive exports + backup push | same project (§4); **never a bare service account** | ⏸ waiting |
| B3 | `CF_TUNNEL_TOKEN`, `CF_ACCESS_TEAM_DOMAIN`, `CF_ACCESS_AUD` | production access/auth | Cloudflare Zero Trust (§5) | ⏸ waiting |
| B4 | Host (≥4 GB RAM, ≥40 GB disk, Docker) | deployment + monthly extract | Hetzner/DO or office box (§6) | ⏸ waiting |
| B5 | Pin current `OVERTURE_RELEASE` + drop the release's full taxonomy file over `data/overture_taxonomy.csv` | real ingest + complete category catalog | Overture releases page (§1) | ⏸ waiting — starter taxonomy subset bundled (D10) |
| B6 | Pin `FSQ_RELEASE` + `FSQ_BASE_URL` | real gap-fill ingest | FSQ OS Places README (§2) | ⏸ waiting |
| B7 | *(optional)* `ANTHROPIC_API_KEY` + console spend limit (~$20) | AI owner extraction + taxonomy assist | console.anthropic.com (§7) | ⏸ optional |
| B8 | *(optional)* `OUTSCRAPER_API_KEY` + prepaid credits | paid coverage top-up | outscraper.com (§8) | ⏸ optional |
| B9 | *(optional)* full SimpleMaps US Cities basic CSV over `data/us_cities.csv` | top-up city fan-out beyond the bundled starter | simplemaps download (§9) | ⏸ optional — starter subset bundled |
| B10 | `APP_SECRET` (any long random string) in prod `.env` | encrypting stored OAuth token / keys at rest | generated by operator | ⏸ waiting |
| B11 | Live-bucket verification of the real Overture/FSQ extract SQL against the pinned release | de-risk D9 schema mapping | run `npm run extract -- --probe` once B5/B6 pinned | ⏸ waiting |
| B12 | Gemfield client roster import (domains/phones) | do-not-prospect suppression seeded | ops → Suppressions → Import (kind=client) | ⏸ waiting |
